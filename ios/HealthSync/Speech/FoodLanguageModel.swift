import Speech

/// Stub — a later milestone should train an `SFCustomLanguageModelData`
/// corpus of food vocabulary (ethnic dishes, brand names, preparation
/// methods) and return the compiled configuration here.
enum FoodLanguageModel {
    static func configuration() -> SFSpeechLanguageModel.Configuration? {
        nil
    }
}
