services:
  api:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - DATABASE_URL=postgres://ladvien:ladvien@192.168.1.104:5432/apple_health_source
      - JWT_SECRET=apple-health-source-jwt-secret-32chars!
      - RUST_LOG=apple_health_api=info,tower_http=info
      - API_HOST=0.0.0.0
      - API_PORT=3000
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
